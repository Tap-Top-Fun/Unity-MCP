name: 6000.0.46f1-standalone

on:
  pull_request:
    branches: [main, dev]
    types: [opened, synchronize, reopened]

  pull_request_target:
    branches: [main, dev]
    types: [opened, synchronize, reopened]

jobs:
  build-and-zip-mcp-server:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Make build script executable
        run: chmod +x ./Unity-MCP-Server/build-all.sh

      - name: Build executables for all platforms
        shell: bash {0}
        run: cd Unity-MCP-Server && ./build-all.sh Release

  # --- UNITY TESTS ---
  # -------------------

  # --- EDIT MODE ---

  test-unity-2022-3-61f1-editmode:
    needs: build-and-zip-mcp-server
    uses: ./.github/workflows/unity_tests.yml
    with:
      projectPath: './Unity-MCP-Plugin'
      unityVersion: '2022.3.61f1'
      testMode: 'editmode'
    secrets: inherit

  test-unity-2023-3-20f1-editmode:
    needs: build-and-zip-mcp-server
    uses: ./.github/workflows/unity_tests.yml
    with:
      projectPath: './Unity-MCP-Plugin'
      unityVersion: '2023.2.20f1'
      testMode: 'editmode'
    secrets: inherit

  test-unity-6000-0-46f1-editmode:
    needs: build-and-zip-mcp-server
    uses: ./.github/workflows/unity_tests.yml
    with:
      projectPath: './Unity-MCP-Plugin'
      unityVersion: '6000.0.46f1'
      testMode: 'editmode'
    secrets: inherit

  # --- PLAY MODE ---

  test-unity-2022-3-61f1-playmode:
    needs: build-and-zip-mcp-server
    uses: ./.github/workflows/unity_tests.yml
    with:
      projectPath: './Unity-MCP-Plugin'
      unityVersion: '2022.3.61f1'
      testMode: 'playmode'
    secrets: inherit

  test-unity-2023-3-20f1-playmode:
    needs: build-and-zip-mcp-server
    uses: ./.github/workflows/unity_tests.yml
    with:
      projectPath: './Unity-MCP-Plugin'
      unityVersion: '2023.2.20f1'
      testMode: 'playmode'
    secrets: inherit

  test-unity-6000-0-46f1-playmode:
    needs: build-and-zip-mcp-server
    uses: ./.github/workflows/unity_tests.yml
    with:
      projectPath: './Unity-MCP-Plugin'
      unityVersion: '6000.0.46f1'
      testMode: 'playmode'
    secrets: inherit

  # --- STANDALONE ---

  test-unity-2022-3-61f1-standalone:
    needs: build-and-zip-mcp-server
    uses: ./.github/workflows/unity_tests.yml
    with:
      projectPath: './Unity-MCP-Plugin'
      unityVersion: '2022.3.61f1'
      testMode: 'standalone'
    secrets: inherit

  test-unity-2023-3-20f1-standalone:
    needs: build-and-zip-mcp-server
    uses: ./.github/workflows/unity_tests.yml
    with:
      projectPath: './Unity-MCP-Plugin'
      unityVersion: '2023.2.20f1'
      testMode: 'standalone'
    secrets: inherit

  test-unity-6000-0-46f1-standalone:
    needs: build-and-zip-mcp-server
    uses: ./.github/workflows/unity_tests.yml
    with:
      projectPath: './Unity-MCP-Plugin'
      unityVersion: '6000.0.46f1'
      testMode: 'standalone'
    secrets: inherit

  # -------------------